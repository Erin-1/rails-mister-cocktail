<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <h1><%= @cocktail.name %></h1>
      <% @cocktail.doses.each do |dose| %>

      <%= dose.description %>
      <%= dose.ingredient.name %>
      <%= link_to "delete dose", dose_path(dose), method: :deletes, class: "btn" %>
      <% end %>

<%= link_to "add a dose", new_cocktail_dose_path(@cocktail) %>


      <%= link_to 'Back', cocktails_path %>
      <!-- link_to 'Edit', cocktail_path(@cocktail)  method: :update  -->

      <%= link_to "Delete the cocktail", cocktail_path(@cocktail),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    </div>
  </div>
</div>


